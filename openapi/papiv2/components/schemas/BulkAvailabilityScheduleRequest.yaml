type: object
required:
  - productCodes
properties:
  productCodes:
    type: array
    maxItems: 500
    description: List of product codes for which to retrieve availability schedules
    items:
      type: string
      pattern: '(?s).*[\S].*'
